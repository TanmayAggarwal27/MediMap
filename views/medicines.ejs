<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Pharmaceutical Inventory</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/medicines.css">
</head>
<body>
  <%- include("navbar") %>
  <div class="container">
    <h2>Pharmaceutical Inventory</h2>

  
    
    
    
    
    

    <form class="search-box" method="GET" action="/all">
      <input type="text" name="q" placeholder="Search by medicine name or salt composition..." />
      <input type="text" name="pincode" placeholder="Enter Pincode..." /> <!-- new field -->
      <button type="submit">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"></circle>
          <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
        </svg>
      </button>
    </form>

    <div class="card-grid">
      <% if (medicines && medicines.length > 0) { %>
        <% medicines.forEach(med => { %>
          <div class="card">
            <div class="card-title"><%= med.name %></div>
            <div class="card-subtitle"><%= med.salt %></div>
            <div class="card-text">
              <p><strong>Price:</strong> ₹<%= med.price %></p>
              <p><strong>Chemist:</strong> <%= med.createdBy.username %></p>
              <p><strong>Phone:</strong>+91-<%= med.createdBy.phoneNumber %></p>
              <p><strong>Address:</strong> <%= med.createdBy.address %></p>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="no-results">
          <p>No medicines found matching your criteria.</p>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>